# Server Configuration
PORT=5000
NODE_ENV=development

# Database Connection (PostgreSQL - Supabase / Local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/parkease?schema=public"

# JWT Authentication
JWT_SECRET="parkease_super_secure_jwt_secret_key_2026_change_in_production"
JWT_EXPIRES_IN="7d"

# Chapa Payment Gateway (Free test key from chapa.co)
CHAPA_SECRET_KEY="CHASECK_TEST-xxxxxxxxxxxxxxxxxxxxxxxx"
CHAPA_WEBHOOK_SECRET="parkease_chapa_webhook_secret"

# Frontend Origin for CORS
CORS_ORIGIN="*"
